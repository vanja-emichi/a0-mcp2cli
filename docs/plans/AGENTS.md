# docs/plans/ DOX

## Purpose
Implementation plans for mcp2cli/MCP-evolution work. Active plans get a
`status: active` frontmatter line; completed plans move to `status: done` with
verification evidence linked, not deleted.
