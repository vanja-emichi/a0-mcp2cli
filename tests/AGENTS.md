# tests/ DOX

## Purpose
Project-level test contracts for `_mcp2cli` integration evidence.

## Local Contracts
- Framework-behavior tests run under `/opt/venv-a0`; CLI workflow tests under `/opt/venv`.
- Plugin unit tests live in the plugin repo (`usr/plugins/_mcp2cli/tests/`); this folder holds project-level integration evidence only.
