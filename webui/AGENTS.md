# webui/ DOX

## Purpose
`config.html` — plugin settings panel: `mcp_mode` dropdown (`cli` / `default`).

## Local Contracts
- Config key is `mcp_mode`; persisted per-plugin config, overridable per project.
