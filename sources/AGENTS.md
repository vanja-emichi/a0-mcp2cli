# sources/ DOX

## Purpose
Read-only upstream reference clones (e.g. `knowsuchagency/mcp2cli` under `sources/github/`).

## Local Contracts
- Never edit content here; record remote, version, and commit in `docs/sources/`.
